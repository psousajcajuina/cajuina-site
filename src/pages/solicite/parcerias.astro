---
import Layout from '@/layouts/PageLayout.astro';
import type { MetaData } from '@/types';
import ReactParceriasForm from '@/components/react/widgets/ReactParceriasForm';
import Banner from '@/components/MiddleBanner.astro';

const metadata: MetaData = {
  title: 'Parcerias',
};

const siteKey = import.meta.env.PUBLIC_GOOGLE_CAPTCHA_SITEKEY;
const apiUrl = import.meta.env.PUBLIC_API_BASE_URL;
---

<Layout metadata={metadata}>
  <section class="first:mt-12 last:mb-12">
    <Banner />
    <div
      class="flex h-fit w-full flex-col items-center justify-center gap-4 p-8"
    >
      <h4
        class="text-caju-heading-primary self-start text-2xl! uppercase md:text-4xl!"
      >
        Sobre as solicitações
      </h4>
      <ul
        class="font-inter flex list-disc flex-col gap-8 px-4 [&_li]:text-[24px]"
      >
        <li>
          <h5 class="text-[32px] font-black">Tipos de solicitações</h5>
          <dd>
            <span class="font-semibold">Doação: </span>Não há expectativa de
            contrapartidas;
          </dd>
          <dd>
            <span class="font-semibold">Patrocínio: </span>Há pagamento em
            produto e existe contrapartida;
          </dd>
          <dd>
            <span class="font-semibold">Parceria: </span>Há pagamento em
            dinheiro e existe contrapartida
          </dd>
        </li>
        <li>
          Pedimos para enviar propostas com mais de 10 DIAS de antecedência da
          data inicial do evento, pois o mesmo será avaliado e respondido dentro
          do prazo de 10 DIAS.
        </li>
        <li>
          Lembre-se de incluir na descrição do projeto o tipo de parceria que
          você espera (divulgação, apoio financeiro, etc.)
        </li>
      </ul>
      <ReactParceriasForm
        client:load
        apiUrl={apiUrl}
        recaptchaSiteKey={siteKey}
      />
    </div>
    <Banner />
  </section>
</Layout>
