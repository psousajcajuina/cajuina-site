---
// YouTube Helper - Ferramenta para converter URLs do YouTube em shortcodes
// Esta página não deve ser indexada por motores de busca
---

<!DOCTYPE html>
<html lang="pt-BR">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="robots" content="noindex, nofollow" />
    <title>Ferramenta YouTube → Shortcode - Cajuína São Geraldo</title>
    <style>
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }

      body {
        font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto,
          'Helvetica Neue', Arial, sans-serif;
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        min-height: 100vh;
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 20px;
        color: #333;
      }

      .container {
        background: white;
        border-radius: 16px;
        box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
        max-width: 700px;
        width: 100%;
        padding: 40px;
      }

      h1 {
        color: #667eea;
        margin-bottom: 10px;
        font-size: 28px;
      }

      .subtitle {
        color: #666;
        margin-bottom: 30px;
        font-size: 16px;
      }

      .bookmarklet-section {
        background: #f8f9fa;
        border: 2px dashed #667eea;
        border-radius: 12px;
        padding: 30px;
        text-align: center;
        margin: 30px 0;
      }

      .bookmarklet-button {
        display: inline-block;
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        color: white;
        padding: 16px 32px;
        border-radius: 8px;
        text-decoration: none;
        font-weight: bold;
        font-size: 18px;
        cursor: move;
        transition: transform 0.2s, box-shadow 0.2s;
        box-shadow: 0 4px 12px rgba(102, 126, 234, 0.4);
      }

      .bookmarklet-button:hover {
        transform: translateY(-2px);
        box-shadow: 0 6px 20px rgba(102, 126, 234, 0.6);
      }

      .drag-instruction {
        margin-top: 15px;
        color: #666;
        font-size: 14px;
      }

      .drag-instruction strong {
        color: #667eea;
      }

      .steps {
        margin: 30px 0;
      }

      .step {
        display: flex;
        align-items: start;
        margin-bottom: 20px;
        padding: 15px;
        background: #f8f9fa;
        border-radius: 8px;
      }

      .step-number {
        background: #667eea;
        color: white;
        width: 32px;
        height: 32px;
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        font-weight: bold;
        flex-shrink: 0;
        margin-right: 15px;
      }

      .step-content h3 {
        color: #333;
        font-size: 16px;
        margin-bottom: 5px;
      }

      .step-content p {
        color: #666;
        font-size: 14px;
        line-height: 1.5;
      }

      .code-block {
        background: #2d2d2d;
        color: #f8f8f2;
        padding: 15px;
        border-radius: 8px;
        font-family: 'Courier New', monospace;
        font-size: 14px;
        margin: 15px 0;
        overflow-x: auto;
      }

      .example {
        background: #e3f2fd;
        border-left: 4px solid #2196f3;
        padding: 15px;
        border-radius: 4px;
        margin: 20px 0;
      }

      .example-title {
        font-weight: bold;
        color: #1976d2;
        margin-bottom: 10px;
      }

      .test-section {
        background: #fff3cd;
        border: 2px solid #ffc107;
        border-radius: 8px;
        padding: 20px;
        margin: 30px 0;
      }

      .test-section h3 {
        color: #856404;
        margin-bottom: 15px;
      }

      .test-input {
        width: 100%;
        padding: 12px;
        border: 2px solid #ddd;
        border-radius: 6px;
        font-size: 14px;
        margin-bottom: 10px;
      }

      .test-button {
        background: #ffc107;
        color: #333;
        border: none;
        padding: 12px 24px;
        border-radius: 6px;
        font-weight: bold;
        cursor: pointer;
        transition: background 0.2s;
      }

      .test-button:hover {
        background: #ffb300;
      }

      .result {
        margin-top: 15px;
        padding: 15px;
        background: white;
        border-radius: 6px;
        font-family: monospace;
        display: none;
      }

      .result.show {
        display: block;
      }

      .success {
        color: #28a745;
      }

      .error {
        color: #dc3545;
      }

      .alert {
        background: #d4edda;
        border: 1px solid #c3e6cb;
        color: #155724;
        padding: 15px;
        border-radius: 6px;
        margin: 20px 0;
      }

      @media (max-width: 600px) {
        .container {
          padding: 20px;
        }

        h1 {
          font-size: 24px;
        }

        .bookmarklet-button {
          font-size: 16px;
          padding: 12px 24px;
        }
      }
    </style>
  </head>
  <body>
    <div class="container">
      <h1>🎬 Ferramenta YouTube → Shortcode</h1>
      <p class="subtitle">
        Converta URLs do YouTube em shortcodes automaticamente para usar no CMS
      </p>

      <div class="bookmarklet-section">
        <a
          href="javascript:(function(){const url=prompt('Cole a URL do YouTube:');if(!url)return;const id=url.match(/(?:v=|youtu\.be\/|embed\/)([a-zA-Z0-9_-]{11})/)?.[1];if(!id){alert('❌ URL inválida do YouTube');return;}const code='[[youtube:'+id+']]';navigator.clipboard.writeText(code).then(()=>{alert('✅ Copiado para área de transferência!\n\n'+code+'\n\nCole no editor com Ctrl+V');}).catch(()=>{prompt('Copie o código abaixo:',code);});})();"
          class="bookmarklet-button"
          onclick="alert('⚠️ Não clique! Arraste este botão para a barra de favoritos do seu navegador.'); return false;"
        >
          📌 YouTube → Shortcode
        </a>
        <p class="drag-instruction">
          <strong>👆 Arraste este botão</strong> para a barra de favoritos do
          seu navegador
        </p>
      </div>

      <div class="alert">
        <strong>💡 Como funciona:</strong> Depois de instalar, clique no
        favorito sempre que quiser adicionar um vídeo. Cole a URL do YouTube e
        o shortcode será copiado automaticamente!
      </div>

      <div class="steps">
        <h2 style="margin-bottom: 20px; color: #667eea">
          📋 Passo a Passo para Instalar
        </h2>

        <div class="step">
          <div class="step-number">1</div>
          <div class="step-content">
            <h3>Mostre a barra de favoritos</h3>
            <p>
              No navegador, pressione <strong>Ctrl+Shift+B</strong> (Windows) ou
              <strong>Cmd+Shift+B</strong> (Mac) para exibir a barra de
              favoritos.
            </p>
          </div>
        </div>

        <div class="step">
          <div class="step-number">2</div>
          <div class="step-content">
            <h3>Arraste o botão roxo</h3>
            <p>
              Clique e arraste o botão "YouTube → Shortcode" acima para a barra
              de favoritos do seu navegador.
            </p>
          </div>
        </div>

        <div class="step">
          <div class="step-number">3</div>
          <div class="step-content">
            <h3>Pronto! Agora use</h3>
            <p>
              Sempre que precisar converter uma URL do YouTube, clique no
              favorito que você acabou de criar.
            </p>
          </div>
        </div>
      </div>

      <div class="example">
        <div class="example-title">📝 Exemplo de Uso no CMS</div>
        <p>
          <strong>1.</strong> Abra o YouTube e copie a URL:
          <code>https://www.youtube.com/watch?v=dQw4w9WgXcQ</code>
        </p>
        <p>
          <strong>2.</strong> No editor do CMS, clique no bookmarklet nos
          favoritos
        </p>
        <p><strong>3.</strong> Cole a URL quando solicitado</p>
        <p>
          <strong>4.</strong> O shortcode
          <code>[[youtube:dQw4w9WgXcQ]]</code> é copiado automaticamente
        </p>
        <p>
          <strong>5.</strong> Cole no editor markdown com
          <strong>Ctrl+V</strong>
        </p>
      </div>

      <div class="test-section">
        <h3>🧪 Testar Conversão</h3>
        <p style="color: #856404; margin-bottom: 15px">
          Teste a conversão de URL para shortcode aqui:
        </p>
        <input
          type="text"
          id="testUrl"
          class="test-input"
          placeholder="Cole uma URL do YouTube (ex: https://www.youtube.com/watch?v=dQw4w9WgXcQ)"
        />
        <button class="test-button" onclick="convertUrl()">
          Converter para Shortcode
        </button>
        <div id="result" class="result"></div>
      </div>

      <div style="margin-top: 30px; padding-top: 30px; border-top: 2px solid #eee">
        <h3 style="color: #667eea; margin-bottom: 15px">
          📚 Formatos de URL Suportados
        </h3>
        <div class="code-block">
          ✅ https://www.youtube.com/watch?v=dQw4w9WgXcQ<br />
          ✅ https://youtu.be/dQw4w9WgXcQ<br />
          ✅ https://www.youtube.com/embed/dQw4w9WgXcQ<br />
          ✅ https://m.youtube.com/watch?v=dQw4w9WgXcQ
        </div>
      </div>

      <div style="margin-top: 30px; text-align: center; color: #999">
        <p>
          Precisa de ajuda? Consulte o
          <a
            href="/EDITOR_GUIDE.md"
            style="color: #667eea; text-decoration: underline"
            >Guia do Editor</a
          >
        </p>
      </div>
    </div>

    <script>
      function convertUrl() {
        const input = document.getElementById('testUrl') as HTMLInputElement | null;
        const result = document.getElementById('result');
        
        if (!input || !result) return;
        
        const url = input.value.trim();

        if (!url) {
          result.className = 'result show error';
          result.textContent = '❌ Por favor, cole uma URL do YouTube';
          return;
        }

        const id = url.match(
          /(?:v=|youtu\.be\/|embed\/)([a-zA-Z0-9_-]{11})/
        )?.[1];

        if (!id) {
          result.className = 'result show error';
          result.textContent =
            '❌ URL inválida. Certifique-se de que é uma URL válida do YouTube.';
          return;
        }

        const shortcode = `[[youtube:${id}]]`;
        result.className = 'result show success';
        result.innerHTML = `
          <strong>✅ Conversão bem-sucedida!</strong><br><br>
          <strong>Shortcode:</strong><br>
          <code style="background: #f0f0f0; padding: 10px; display: block; margin-top: 10px; border-radius: 4px;">${shortcode}</code>
          <br>
          <button 
            onclick="navigator.clipboard.writeText('${shortcode}').then(() => alert('✅ Copiado!'))" 
            style="margin-top: 10px; padding: 8px 16px; background: #28a745; color: white; border: none; border-radius: 4px; cursor: pointer;"
          >
            📋 Copiar Shortcode
          </button>
        `;
      }

      // Permite pressionar Enter para converter
      const testUrlInput = document.getElementById('testUrl');
      if (testUrlInput) {
        testUrlInput.addEventListener('keypress', function (e) {
          if (e.key === 'Enter') {
            convertUrl();
          }
        });
      }
    </script>
  </body>
</html>
