---
// Componente skeleton puramente Astro (SSR)
---

<div
  class="relative h-[341px] w-full overflow-hidden md:h-[700px]"
  aria-hidden="true"
>
  {/* Background com shimmer */}
  <div
    class="shimmer-bg absolute inset-0 bg-linear-to-r from-gray-200 via-gray-100 to-gray-200"
  >
  </div>

  {/* Content container */}
  <div
    class="relative z-10 flex h-full items-center px-[clamp(1.5rem,4vw,3rem)]"
  >
    <div
      class="mx-auto flex w-full max-w-7xl flex-col gap-[clamp(0.75rem,2vw,1.5rem)]"
    >
      {/* Subtitle */}
      <div
        class="h-[clamp(20px,3vw,28px)] w-[clamp(120px,40%,300px)] rounded-md bg-gray-300/60"
      >
      </div>

      {/* Title */}
      <div
        class="h-[clamp(32px,5vw,48px)] w-[clamp(200px,70%,600px)] rounded-md bg-gray-300/60"
      >
      </div>

      {/* Description */}
      <div
        class="h-[clamp(16px,2.5vw,20px)] w-[clamp(250px,85%,700px)] rounded-md bg-gray-300/60"
      >
      </div>
    </div>
  </div>

  {/* Pagination bullets */}
  <div
    class="absolute bottom-[clamp(1rem,3vw,1.5rem)] left-1/2 z-20 flex -translate-x-1/2 gap-2"
  >
    <span class="h-3 w-3 rounded-full bg-gray-300/50"></span>
    <span class="h-3 w-3 rounded-full bg-gray-300/50"></span>
    <span class="h-3 w-3 rounded-full bg-gray-300/50"></span>
  </div>
</div>

<style>
  @keyframes shimmer {
    0% {
      background-position: -200% 0;
    }
    100% {
      background-position: 200% 0;
    }
  }

  .shimmer-bg {
    background-size: 200% 100%;
    animation: shimmer 2s infinite linear;
  }
</style>
