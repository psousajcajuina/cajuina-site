# ===========================================
# Cajuína CMS Auth - Environment Variables
# ===========================================
# Server Configuration (standalone only)
NODE_ENV=development
HOST=localhost
PORT=3000
# Logging
LOG_LEVEL=info
# Security
# Set to "1" for local development to disable Secure cookie attribute
INSECURE_COOKIES=1

# ===========================================
# OAuth Providers
# ===========================================
# Allowed Domains (comma-separated, supports wildcards)
# Example: *.example.com,anotherdomain.com
ALLOWED_DOMAINS=localhost,cajuinasaogeraldo.com.br,*.cajuinasaogeraldo.com.br
# GitHub OAuth App
# Create at: https://github.com/settings/developers
GITHUB_CLIENT_ID=your_github_client_id
GITHUB_CLIENT_SECRET=your_github_client_secret
GITHUB_HOSTNAME=github.com
# GitLab OAuth App
# Create at: https://gitlab.com/-/profile/applications
GITLAB_CLIENT_ID=your_gitlab_client_id
GITLAB_CLIENT_SECRET=your_gitlab_client_secret
GITLAB_HOSTNAME=gitlab.com
# Bitbucket OAuth (not yet implemented)
# BITBUCKET_CLIENT_ID=your_bitbucket_client_id
# BITBUCKET_CLIENT_SECRET=your_bitbucket_client_secret
# BITBUCKET_HOSTNAME=bitbucket.com

# ===========================================
# Notes
# ===========================================
# 1. At least one OAuth provider (GitHub or GitLab) must be configured
# 2. For Cloudflare Workers, set these as secrets using wrangler:
#    wrangler secret put GITHUB_CLIENT_SECRET
# 3. For standalone/Docker, create a .env file with these variables
# 4. Never commit .env files with real credentials to git
